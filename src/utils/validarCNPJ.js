// Validação desativada - sempre retorna true
export function validarCNPJ(cnpj) {
  return true;
}


export const isValidCNPJ = (cnpj) => {
  return true;
};

/**
 * Validates CEP format - desativada
 */
export const isValidCEP = (cep) => {
  return true;
};

export default validarCNPJ;
